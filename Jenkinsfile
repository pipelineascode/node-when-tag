node {
        stage('Build Master') {
            if(env.BRANCH_NAME == 'master'){
                echo 'Building master'
            }
        }
		
        stage('Build Dev') {
            if(env.BRANCH_NAME == 'dev'){
                echo 'Building dev'
            }
        }
}